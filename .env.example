# Lead delivery destination. The platform will NOT claim a request was received
# until this endpoint returns a successful HTTP response.
INTEX_LEADS_WEBHOOK_URL=
INTEX_LEADS_WEBHOOK_SECRET=

# Admin dashboard is disabled by default and returns 404 unless explicitly enabled.
INTEX_ADMIN_ENABLED=false
