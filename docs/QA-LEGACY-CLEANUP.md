# QA Legacy Cleanup

This marker exists only to force the current `main` state through pull-request CI after the 2026-08-24 legacy redirect correction and removal of obsolete generic roofing/service animation stylesheets.

Expected gate: `npm run verify`.
